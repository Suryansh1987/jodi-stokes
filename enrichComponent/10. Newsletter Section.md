# 10. Newsletter Section

Status: Not Started

## Current Component

- `components/sections/home/newsletter-section.tsx`

## MCP Candidates Searched

| MCP Server | Exact Component Name | Fit |
|---|---|---|
| `magic` | `Newsletter Card` | Strong fit for a clean email capture card with focused form hierarchy. |
| `magic` | `Newsletter Signup` | Richer animated option, but may introduce unnecessary confetti/heavier behavior. |
| `magic` | `News letter` | Simple fallback if the richer cards do not match the brand. |
| `magicui` | `noise-texture-demo-2` | Strong inspiration match for newsletter signup card. |
| `magicui` | `dot-pattern` | Good lightweight background alternative. |
| `magicui` | `animated-gradient-text` | Possible accent for `Monday Note`, but use sparingly. |
| `magicui` | `blur-fade` | Good reveal wrapper, not a full newsletter treatment. |

## Proposed Replacement

- Prefer 21st.dev `Newsletter Card` as the primary replacement direction.
- Keep the existing form semantics, email label, local-only confirmation behavior, and provider `[TODO]`.
- Avoid the heavier `Newsletter Signup` effects unless the final design explicitly wants celebratory submission animation.

## MCP Server Used

- `magic`
- `magicui`

## Exact Component Name From MCP Search

- `Newsletter Card`

## Why It Was Chosen

21st.dev `Newsletter Card` is a direct newsletter component and is cleaner than using a Magic UI texture example as the core layout.

## Estimated Implementation Complexity

Medium

## Dependencies

- 21st.dev `Newsletter Card`
- Existing shadcn `Input` and `Button`
- Existing `lucide-react` icons
- Optional `Newsletter Signup` dependency check if that richer variant is selected later

## Are Motion Animations Alone Sufficient?

No. The enrichment is a surface/design treatment plus form preservation, not only animation.

## Acceptance Notes

- Preserve the email label and submit behavior.
- Keep `[PLACEHOLDER]` / `[TODO]` comments for newsletter provider wiring.
- Do not make the texture reduce text/input contrast.
