# 1. Hero Section

Status: Completed

## Current Component

- `components/sections/home/hero-section.tsx`

## MCP Candidates Searched

| MCP Server | Exact Component Name | Fit |
|---|---|---|
| `magicui` | `hero-video-dialog` | Strong fit for the existing visual-only play button. |
| `magicui` | `hero-video-dialog-demo-top-in-bottom-out` | Useful example for entrance/exit animation. |
| `magicui` | `animated-gradient-text` | Good accent candidate for the script/accent headline, but not a full hero replacement. |
| `magicui` | `blur-fade` | Good reveal wrapper, but not specific enough for video behavior. |
| `magic` | `PulseFit hero` | Strong fitness-specific full hero candidate with navigation, CTAs, social proof, and program carousel. |
| `magic` | `Hero with group of images, text and two buttons` | Good generic fallback, less brand-specific than `PulseFit hero`. |

## Proposed Replacement

- Prefer adapting 21st.dev `PulseFit hero` only if the future implementation accepts a high-complexity hero refactor.
- Keep Magic UI `hero-video-dialog` as a smaller enhancement if the goal is only to replace the current play button placeholder.
- Preserve the Jodi portrait, current headline copy, stats, CTAs, and reference dark editorial palette.

## MCP Server Used

- `magic`
- `magicui`

## Exact Component Name From MCP Search

- `PulseFit hero`

## Why It Was Chosen

21st.dev `PulseFit hero` is fitness-specific and gives a richer hero direction than Magic UI `hero-video-dialog`. It includes program-card motion and fitness imagery patterns, but it should be adapted rather than copied wholesale because it also includes its own nav and light palette.

## Estimated Implementation Complexity

High

## Dependencies

- 21st.dev `PulseFit hero`
- `framer-motion`
- Existing `next/image` assets should replace remote sample images.
- Optional fallback: Magic UI `hero-video-dialog` with `motion`.

Implementation note: the final adapted hero did not require adding `framer-motion` or `motion`; it uses existing React state, CSS animations, `next/image`, and `lucide-react`.

## Are Motion Animations Alone Sufficient?

No. Motion is part of the visual effect, but the actual work is adapting a full fitness hero to the existing content model, image assets, and reference layout.

## Acceptance Notes

- Keep `next/image` priority behavior for the hero portrait.
- Preserve `[PLACEHOLDER]` / `[TODO]` comments until a real video asset is provided.
- Dialog must be keyboard accessible.

## Completion Evidence

- Adapted the selected `PulseFit hero` direction in `components/sections/home/hero-section.tsx` with a richer coaching-method card stack, deterministic reveal motion, preserved portrait/CTAs/stats/chips, and an accessible intro preview dialog.
- Added supporting hero card and video-preview content in `lib/content/home.ts`.
- Updated `app/globals.css` for the enriched hero layout, cards, dialog, focus states, and reduced-motion behavior.
- Updated `test/home.spec.ts` to cover the intro dialog open/focus/Escape-close path.
- Dependency note: no new packages were installed.
- Verification:
  - `npm run lint` - passed.
  - `npm run build` - passed.
  - `npm run test:e2e` - blocked on `127.0.0.1:3000` already in use.
  - `PORT=3001 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 npm run test:e2e` - passed, 11 passed and 3 skipped.
