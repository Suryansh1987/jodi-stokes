# 11. Footer Section

Status: Completed

## Current Component

- `components/section/shared/site-footer.tsx`

## MCP Candidates Searched

| MCP Server | Exact Component Name | Fit |
|---|---|---|
| `magic` | `Large Name Footer` | Strong fit for a brand-forward footer with oversized name treatment, link groups, socials, and legal content. |
| `magic` | `Footer` | Good conventional footer fallback with newsletter/link groups. |
| `magic` | `footer` | Rich footer candidate, but may be too platform-like for this lifestyle storefront. |
| `magicui` | `dot-pattern` | Strong subtle background texture candidate. |
| `magicui` | `grid-pattern` | Possible, but too rigid for a lifestyle/footer area. |
| `magicui` | `animated-grid-pattern` | Rejected unless motion is very subtle. |
| `magicui` | `marquee` | Possible for footer announcements, but duplicative with topbar. |

## Proposed Replacement

- Prefer 21st.dev `Large Name Footer` as the primary replacement direction.
- Preserve current brand copy, link groups, social links, legal text, and placeholder comments.
- Use Magic UI `dot-pattern` only as a subtle background primitive if it improves depth without reducing readability.

## MCP Server Used

- `magic`
- `magicui`

## Exact Component Name From MCP Search

- `Large Name Footer`

## Why It Was Chosen

21st.dev `Large Name Footer` is a full footer component and better matches a brand-forward lifestyle site than a background texture alone.

## Estimated Implementation Complexity

Medium

## Dependencies

- 21st.dev `Large Name Footer`
- Existing `next/link`
- Existing shadcn `Button` if the selected footer includes calls to action
- Custom brand/social icons may need to replace sample icons
- Optional Magic UI `dot-pattern`

## Are Motion Animations Alone Sufficient?

No. This task does not need motion; static texture is the safer enrichment.

## Acceptance Notes

- Footer links and placeholders must remain intact.
- Do not duplicate the topbar marquee in the footer.
- Texture must not interfere with focus-visible states.

## Implementation Notes

- Adapted the 21st.dev `Large Name Footer` direction in the existing shared server footer instead of installing a generated component.
- Preserved the existing brand logo link, description, social links, Shop/Train/Studio link groups, legal links, copyright text, and placeholder/TODO comments.
- Added deterministic footer content fields for the large brand treatment and kicker copy.
- Reworked footer CSS with a large clipped brand-name treatment, subtle static texture, carded link groups, responsive wrapping, and preserved focus-visible states.
- Updated Playwright footer coverage for the large-name element, footer kicker, social link count, and link group count.

## Verification Evidence

- `npm run lint` passed.
- `npm run build` passed after replacing an invalid footer background utility with `bg-surface-1/75`.
- `npm run test:e2e` could not start because `http://127.0.0.1:3000` was already in use.
- `PORT=3001 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 npm run test:e2e` passed with 11 passed and 3 skipped.
