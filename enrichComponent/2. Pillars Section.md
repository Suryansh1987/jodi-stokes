# 2. Pillars Section

Status: Not Started

## Current Component

- `components/sections/home/pillars-section.tsx`

## MCP Candidates Searched

| MCP Server | Exact Component Name | Fit |
|---|---|---|
| `magicui` | `bento-grid` | Strong fit for three editorial method cards. |
| `magicui` | `magic-card` | Good hover effect, but less structural than bento. |
| `magicui` | `border-beam` | Useful accent only; too decorative for all pillars. |
| `magicui` | `blur-fade` | Good reveal wrapper only. |
| `magic` | `Features 10` | Strong rich feature-card section with decorated cards and mixed grid hierarchy. |
| `magic` | `Features` | Usable fallback, but more SaaS/table-oriented than the pillars need. |

## Proposed Replacement

- Replace the current custom `.pillar-grid` layout with an adapted 21st.dev `Features 10` card structure while preserving the three pillar records.
- Use existing lucide arrows or icons rather than adding new decorative icon systems.

## MCP Server Used

- `magic`
- `magicui`

## Exact Component Name From MCP Search

- `Features 10`

## Why It Was Chosen

21st.dev `Features 10` is a richer semantic fit than Magic UI `bento-grid` because it already provides decorated feature cards and mixed emphasis while still allowing the Move/Live/Eat pillar content to stay data-driven.

## Estimated Implementation Complexity

Medium

## Dependencies

- 21st.dev `Features 10`
- Existing shadcn `Card`
- `lucide-react`
- Existing pillar content records.

## Are Motion Animations Alone Sufficient?

No. The improvement is primarily card composition and hierarchy; Motion animation is optional.

## Acceptance Notes

- Preserve `#about` hash target behavior.
- Keep cards readable at mobile widths.
- Avoid oversized cards that push the About section too far below the fold.
