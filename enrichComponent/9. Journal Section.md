# 9. Journal Section

Status: Not Started

## Current Component

- `components/sections/home/journal-section.tsx`

## MCP Candidates Searched

| MCP Server | Exact Component Name | Fit |
|---|---|---|
| `magic` | `Blog 8` | Strong fit for editorial post previews with image/text hierarchy and metadata. |
| `magic` | `Blog 7` | Good card-grid fallback. |
| `magic` | `Cards` | Generic fallback; less article-specific. |
| `magicui` | `bento-grid` | Strong fit for editorial article cards. |
| `magicui` | `magic-card` | Good hover polish, but less useful than layout. |
| `magicui` | `dot-pattern` | Background texture candidate only. |
| `magicui` | `blur-fade` | Reveal wrapper only. |

## Proposed Replacement

- Prefer 21st.dev `Blog 8` as the primary replacement direction.
- Preserve current journal post titles, categories, dates, descriptions, and placeholder links.
- Use Magic UI `bento-grid` only if the implementation needs a custom featured/secondary layout instead of the ready-made blog section.

## MCP Server Used

- `magic`
- `magicui`

## Exact Component Name From MCP Search

- `Blog 8`

## Why It Was Chosen

21st.dev `Blog 8` is article-specific and gives the section the editorial hierarchy it needs without forcing a generic card layout.

## Estimated Implementation Complexity

Medium

## Dependencies

- 21st.dev `Blog 8`
- Existing shadcn `Card`
- Existing `lucide-react` icons

## Are Motion Animations Alone Sufficient?

No. The improvement is a layout/content hierarchy change, not just animation.

## Acceptance Notes

- Do not make journal cards look like product cards.
- Maintain readable dates and categories.
- Keep cards responsive at mobile widths.
