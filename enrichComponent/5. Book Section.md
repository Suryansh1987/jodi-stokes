# 5. Book Section

Status: Completed

## Current Component

- `components/sections/home/book-section.tsx`

## MCP Candidates Searched

| MCP Server | Exact Component Name | Fit |
|---|---|---|
| `magicui` | `border-beam` | Strong fit around the book cover or offer panel. |
| `magicui` | `animated-gradient-text` | Possible accent for the offer badge only. |
| `magicui` | `blur-fade` | Good reveal wrapper, but less distinctive. |
| `magicui` | `animated-grid-pattern` | Possible background, but may distract from the book cover. |
| `magic` | `Feature Showcase` | Strong rich split feature candidate with text, chips, accordion, CTAs, and media panel. |
| `magic` | `Feature 72` | Good product-feature fallback with image cards. |

## Proposed Replacement

- Prefer adapting 21st.dev `Feature Showcase` if the book section should become a richer product-feature experience.
- Preserve the current 3D book cover, price row, feature list, and book CTAs.
- Use Magic UI `border-beam` only as a lower-risk accent around the existing book cover.

## MCP Server Used

- `magic`
- `magicui`

## Exact Component Name From MCP Search

- `Feature Showcase`

## Why It Was Chosen

21st.dev `Feature Showcase` is a better section-level candidate than Magic UI `border-beam` because the book section is a product feature with media, benefits, and purchase CTAs. It gives more structure than a decorative border effect.

## Estimated Implementation Complexity

High

## Dependencies

- 21st.dev `Feature Showcase`
- shadcn/Radix `tabs`
- shadcn/Radix `accordion`
- shadcn `Button`, `Badge`, `Card`
- Existing `next/image` book cover asset.
- Optional fallback: Magic UI `border-beam` with `motion`.

## Are Motion Animations Alone Sufficient?

No. The preferred candidate needs structural adaptation. Motion alone is only sufficient for the fallback border accent.

## Acceptance Notes

- Do not remove the existing `next/image` book cover.
- Keep book CTAs marked `[PLACEHOLDER]` / `[TODO]` until checkout is real.

## Implementation Notes

- Adapted the selected 21st.dev `Feature Showcase` direction into a static, server-rendered product feature layout for the book section.
- Preserved the existing `next/image` 3D book cover, pricing row, feature checklist, and placeholder book CTAs.
- Added deterministic book highlight chips and an "inside the book" detail stack without introducing fake tabs, accordion behavior, or new generated UI files.
- Updated responsive styling so the media panel and CTA stack remain stable across desktop and mobile layouts.
- Expanded Playwright coverage for the new book highlights/details while retaining the existing image, price, offer, feature, CTA, and overflow assertions.

## Completion Evidence

- `npm run lint` - passed.
- `npm run build` - passed with Next.js 16.2.9.
- `npm run test:e2e` - failed because `http://127.0.0.1:3000` was already in use; no process was killed.
- `PORT=3001 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 npm run test:e2e` - passed (11 passed, 3 skipped).
