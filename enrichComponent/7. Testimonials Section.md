# 7. Testimonials Section

Status: Completed

## Current Component

- `components/sections/home/testimonials-section.tsx`

## MCP Candidates Searched

| MCP Server | Exact Component Name | Fit |
|---|---|---|
| `magic` | `Testimonials` | Strong fit for a richer editorial testimonial section with stronger hierarchy. |
| `magic` | `Testimonials 1` | Good simpler testimonial grid fallback. |
| `magic` | `Testimonial Card` | Useful single-card fallback if a carousel/card system is preferred. |
| `magicui` | `marquee` | Strong fit for client story carousel behavior. |
| `magicui` | `client-tweet-card` | Rejected; testimonial data is not tweet-shaped. |
| `magicui` | `tweet-card` | Rejected; social-post UI does not match client stories. |
| `magicui` | `magic-card` | Good card polish candidate if marquee is not used. |

## Proposed Replacement

- Prefer 21st.dev `Testimonials` as the primary replacement direction.
- Preserve current quote content and client metadata.
- Use Magic UI `marquee` only if continuous motion is intentionally desired; otherwise avoid making testimonials harder to read.

## MCP Server Used

- `magic`
- `magicui`

## Exact Component Name From MCP Search

- `Testimonials`

## Why It Was Chosen

21st.dev `Testimonials` is a better fit than a marquee because this section needs readable social proof and editorial hierarchy more than constant motion.

## Estimated Implementation Complexity

Medium

## Dependencies

- 21st.dev `Testimonials`
- Existing shadcn `Card`
- shadcn/Radix `Avatar` if the chosen component uses profile media

## Are Motion Animations Alone Sufficient?

No. The section needs layout and hierarchy changes, not only animation.

## Acceptance Notes

- Quotes must remain readable and keyboard-neutral.
- Avoid fast animation.
- Do not replace testimonial content with tweet-style cards.

## Implementation Notes

- Adapted the selected 21st.dev `Testimonials` direction into a static, server-rendered editorial proof layout.
- Preserved current quote content, names, initials, client details, 5-star ratings, and result metrics.
- Added a featured first testimonial with stronger quote/result hierarchy and a supporting stack for the remaining stories.
- Added deterministic tenure/focus metadata derived from existing client story context.
- Avoided marquee/carousel behavior, fast animation, `framer-motion`, and generated Avatar files.
- Expanded Playwright coverage for card count, featured treatment, quotes, initials, details, metadata, ratings, results, and overflow.

## Completion Evidence

- `npm run lint` - passed.
- `npm run build` - passed with Next.js 16.2.9.
- `npm run test:e2e` - failed because `http://127.0.0.1:3000` was already in use; no process was killed.
- `PORT=3001 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 npm run test:e2e` - passed (11 passed, 3 skipped).
