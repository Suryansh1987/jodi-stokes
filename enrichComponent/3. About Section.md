# 3. About Section

Status: Not Started

## Current Component

- `components/sections/home/about-section.tsx`

## MCP Candidates Searched

| MCP Server | Exact Component Name | Fit |
|---|---|---|
| `magicui` | `blur-fade` | Strong fit for subtle photo/copy reveal. |
| `magicui` | `dot-pattern` | Good background texture candidate if kept subtle. |
| `magicui` | `animated-grid-pattern` | Possible background, but more technical than lifestyle/editorial. |
| `magicui` | `animated-gradient-text` | Possible accent, but likely too flashy for biography copy. |
| `magic` | `About 3` | Strong full about-section candidate with image grid, breakout card, and stats. |
| `magic` | `About` | Simpler feature-grid candidate, less suited to Jodi's biography. |

## Proposed Replacement

- Prefer adapting 21st.dev `About 3` if the section should become more editorial and image-rich.
- Keep Magic UI `blur-fade` only as a low-risk fallback when the existing layout should stay intact.
- Preserve Jodi biography copy, portrait, signature, and credentials.

## MCP Server Used

- `magic`
- `magicui`

## Exact Component Name From MCP Search

- `About 3`

## Why It Was Chosen

21st.dev `About 3` is a stronger section-level candidate than Magic UI `blur-fade` because it provides image hierarchy, a breakout content panel, and achievement/stat slots that map to credentials and coaching proof points.

## Estimated Implementation Complexity

Medium

## Dependencies

- 21st.dev `About 3`
- Existing `Button` if CTA is retained.
- Existing `next/image` assets should replace sample remote images.
- Optional fallback: Magic UI `blur-fade` with `motion`.

## Are Motion Animations Alone Sufficient?

No. Motion alone is sufficient only for the fallback. The preferred 21st.dev option is a structural about-section enhancement.

## Acceptance Notes

- Motion must respect reduced-motion preferences.
- Do not obscure the portrait with heavy animated backgrounds.
