# 6. Regression Verification

Status: Not Started  
Depends On: Tasks 1-5

## Goal

Perform the final cross-cutting verification pass, remove stale mock assertions/content left by earlier tasks, and record launch blockers without broadening scope.

## Context

- `test/home.spec.ts` currently covers the entire one-page experience in production mode across desktop and mobile.
- Existing tests strongly assert mock names, prices, filters, cart controls, book `href="#"` links, fake coaching details, footer links, and current metadata.
- Shared helpers already cover loaded images, horizontal overflow, and overlap.
- Playwright owns `npm run build && npm run start`, does not reuse manual servers, and can run on an explicit alternate port.

## Files / Areas Likely Involved

- `test/home.spec.ts`
- Additional focused files under `test/` only if splitting the registration/product coverage improves clarity
- `test/support/assertions.ts`
- `playwright.config.ts` only if required for deterministic email endpoint mocking or environment setup
- All production files changed by Tasks 1-5, but only for defects found during verification
- All numbered task evidence blocks and `programsProductsRefactor/taskTracker.md`

## Required Changes

- Consolidate tests around current behavior rather than implementation-specific CSS art from the deleted storefront.
- Cover exactly one program, exactly four products, featured book image, missing/external product URL states, registration fields/acknowledgement, server validation, mocked email success/failure, navigation, footer, metadata, and responsive behavior.
- Preserve relevant hero, about, testimonials, journal, newsletter, accessibility, image-loading, mobile-menu, and overflow regression coverage.
- Ensure automated tests never call the real email provider or send to Jodi.
- Remove stale test assertions for former products/programs, fake prices/dates/seats, cart/favorites/filters/colors/shipping, `#shop`, `#book`, and book `href="#"` behavior.
- Search all production source, tests, and active content for stale mock names/actions. Reference HTML and historical completed task documents may retain historical evidence and do not need rewriting.
- Inspect desktop and mobile screenshots for blank/missing media, text overflow, overlap, unstable card sizes, registration states, and section spacing.
- Update completion evidence task-by-task using the exact commands actually run; do not claim a provider smoke test if credentials were unavailable.

## Explicit Non-Goals

- No new features, visual redesign, CMS, database, analytics, blog/newsletter work, or speculative cleanup.
- Do not rewrite historical `initializeTodo/`, `enrichComponent/`, or `reference-html/` artifacts to erase old mock history.
- Do not weaken assertions merely to make failures pass; fix scoped regressions or record a genuine blocker.

## Data / Client Requirements

- Final launch verification requires approved program copy/agreement text, product URLs/prices/descriptions/photos, and verified email provider configuration.
- If these remain missing, verify the honest unavailable states and leave launch status blocked rather than fabricating final content.

## Edge Cases

- Run production Playwright on an explicit alternate port if the default is occupied; do not kill another process or reuse it.
- Validate both products with URLs and products without URLs if the final dataset contains both.
- Verify a failed registration retains user input and a successful mocked registration resets only appropriate controls.
- Check keyboard focus and status announcements, not only visible text.

## Verification

- `npm run lint`
- `npm run build`
- `npm run test:e2e` or the documented alternate-port equivalent using Playwright's managed server
- `git diff --check`
- A scoped `rg` search for every former mock name/action and obsolete anchor
- Manual desktop/mobile screenshot inspection
- One controlled staging email smoke test only when approved credentials and sender domain are available

## Completion Criteria

- Lint, build, and deterministic production-mode Playwright pass.
- Active code and tests contain no stale mock Programs/Products commerce behavior.
- Responsive, accessibility, navigation, external-link, and form-state behavior are verified.
- All tasks contain exact completion evidence.
- Remaining client/deployment dependencies are clearly marked as blockers; otherwise the tracker may be marked complete.

## Tracker Update

Record final command output summaries, test counts, alternate port if used, screenshot viewport results, stale-string search results, provider smoke-test status, unresolved blockers, and overall completion status in `taskTracker.md`.

## Completion Evidence

Status: Not Started

Implemented files: None.  
Verification: Not Run.  
Notes: Add evidence only after Task 6 is explicitly implemented.
