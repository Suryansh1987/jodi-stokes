# 5. Shop Section and Product Cards

Status: Completed
Mandatory: Yes
Depends On: Tasks 0-2

## Goal

Implement the shop category bar and eight mock merchandise cards from the reference design.

## Reference Mock Findings

- Shop section has category chips: All, Apparel, Gear, Hydration, Journals, Bundles.
- Products are mock/CSS-rendered items, not real product photos.
- Cards include badges, heart/save buttons, category text, product name, price, and color swatches.
- Product examples include tee, bottle, mat, cap, crew, resistance bands, journal, and tote bundle.

## Implementation Scope

- Create `components/sections/home/shop-section.tsx`.
- Create smaller internal components if useful, such as `ProductCard` and CSS-art helpers, but keep them route-specific unless reused elsewhere.
- Use shadcn `Card` if it fits the layout.
- Use buttons for category chips and heart actions.
- Use `lucide-react` Heart icon.
- Mark category filtering, save/favorite, and cart behavior with `[PLACEHOLDER]`.
- Mark real ecommerce state, product pages, inventory, and checkout with `[TODO]`.

## Files To Inspect

- `reference-html/index.html` Shop section.
- `lib/content/home.ts`
- `components/ui/card.tsx`

## Acceptance Criteria

- The grid renders 4 columns on desktop, then responsive smaller grids.
- Product visual placeholders intentionally resemble the mock CSS product art.
- Color swatches are visible and have accessible labels or hidden text where needed.
- Buttons have accessible names.
- No actual ecommerce behavior is implied without marker comments.

## Tests

- Run `npm run lint`.
- Run `npm run build`.
- Manual responsive check at desktop and mobile widths.

## Completion Evidence

Status: Completed

Implemented files:

- `components/sections/home/shop-section.tsx`
- `app/globals.css`
- `test/home.spec.ts`
- `eslint.config.mjs`
- `initializeTodo/5. Shop Section and Product Cards.md`
- `initializeTodo/taskTracker.md`

Frontend command:

- `npm run lint`
- `npm run build`
- `PORT=3001 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 npm run test:e2e`
- `git diff --check`

Frontend result:

- Passed.

Manual verification:

- Shop section now renders the reference-style category chip bar with accessible buttons.
- Eight mock merchandise cards render from `shopProducts`.
- Cards include badges, save buttons, category text, semantic product headings, prices, compare-at pricing, and labeled color swatches.
- CSS product art variants render for tee, bottle, mat, cap, crew, resistance bands, journal, and tote bundle.
- Responsive grid rules cover desktop, tablet, and mobile layouts.
- Playwright runs in production mode through its managed server lifecycle on port `3001`.

Evidence:

- Existing placeholder shop cards were replaced with shadcn `Card` composition and route-local `ProductCard`/`ProductArt` helpers.
- `[PLACEHOLDER]` and `[TODO]` markers were added near visual-only category filtering and favorite behavior.
- Playwright coverage was added for shop heading, category buttons, products, badges, pricing, color labels, art variants, save buttons, and horizontal overflow.
- `eslint.config.mjs` now ignores Playwright output directories so lint is deterministic when reports exist or are absent.

Caveats:

- Category filtering and save/favorite behavior remain visual-only.
- Real ecommerce state, product pages, inventory, checkout, and persistence are deferred.
