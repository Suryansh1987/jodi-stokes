# 1. Public Layout, Header, Mobile Navigation

Status: Completed  
Mandatory: Yes  
Depends On: Task 0

## Goal

Recreate the mock topbar, sticky navigation, brand mark, desktop links, action buttons, and mobile menu as shared public layout components.

## Reference Mock Findings

- The topbar is a mint marquee with repeated promotional messages.
- The nav is sticky, translucent black, blurred, and bordered.
- Brand shows circular `JS` mark plus `JODI STOKES` and small service tagline.
- Desktop links anchor to `about`, `programs`, `book`, `shop`, `journal`, and `coaching`.
- Actions include search, cart with a `3` badge, and `Book a call`.
- Mobile replaces links with a hamburger and fullscreen dark menu.
- The mock uses inline SVG icons; Next implementation should use `lucide-react` icons where possible.

## Implementation Scope

- Create shared components in `components/section/shared/`:
  - `brand-logo.tsx`
  - `topbar.tsx`
  - `site-header.tsx`
  - `mobile-nav.tsx`
- Add header/nav to the public route layout, either `app/(public)/layout.tsx` or the home composition if a route layout is not yet useful.
- Use `next/link` for route/hash navigation.
- Use `lucide-react` for Search, ShoppingBag, Menu, X, ArrowRight, and social icons when available.
- Make the mobile nav a client component only where interactivity is required.
- Mark fake search and cart behavior with `[PLACEHOLDER]`.
- Mark future cart/search implementation with `[TODO]`.

## Files To Inspect

- `reference-html/index.html` lines around the topbar, nav, and mobile nav.
- `app/layout.tsx`
- `app/(public)/page.tsx`
- `components/ui/card.tsx`

## Acceptance Criteria

- Header matches the mock hierarchy and visual weight.
- Desktop nav is visible at larger widths and hidden on mobile.
- Mobile menu opens/closes, updates `aria-expanded`, and prevents inaccessible hidden content.
- Anchor links target section IDs used by the page sections.
- Header remains sticky without overlapping first viewport content incorrectly.

## Tests

- Run `npm run lint`.
- Run `npm run build`.
- Manual desktop check at 1440px width.
- Manual mobile check at 390px width, including menu open/close.

## Completion Evidence

Status: Completed

Implemented files:
- `app/(public)/layout.tsx`
- `components/section/shared/brand-logo.tsx`
- `components/section/shared/topbar.tsx`
- `components/section/shared/site-header.tsx`
- `components/section/shared/mobile-nav.tsx`
- `components/section/shared/.gitkeep` removed
- `app/globals.css`
- `initializeTodo/1. Public Layout Header Mobile Navigation.md`
- `initializeTodo/taskTracker.md`

Frontend command:
- `npm run lint`
- `npm run build`

Frontend result: Passed

Manual verification:
- Inspected `.next/server/app/index.html` after build and confirmed the generated page includes `topbar`, `site-header`, desktop nav links, search/cart buttons, CTA, mobile menu button with `aria-expanded="false"`, and closed `mobile-nav`.
- Confirmed responsive CSS switches desktop navigation/CTA and mobile menu behavior at the mock's `720px` breakpoint.
- Browser viewport visual checks at 1440px and 390px were not run in this environment.

Evidence:
- Added `app/(public)/layout.tsx` so all public routes render the shared topbar and site header before page content.
- Added a shared `BrandLogo` matching the mock's `JS` mark, name, and service tagline.
- Added `Topbar` with duplicated marquee messages and reduced-motion support.
- Added `SiteHeader` with sticky translucent styling, desktop hash links, search/cart icon buttons, cart bubble, and `Book a call` CTA.
- Added `MobileNav` as the only client component for menu open/close state, `aria-expanded`, `aria-hidden`, body scroll locking, and close-on-link-click behavior.
- Added `[PLACEHOLDER]` and `[TODO]` comments for visual-only search, cart, cart count, and temporary social links.
- Extended `app/globals.css` with header, marquee, mobile-nav, breakpoint, focus-visible, and cart-bubble styles.
- `npm run lint` passed.
- `npm run build` passed and reported a single static `/` route plus `/_not-found`.

Caveats:
- Social links are temporary text-based placeholders (`IG`, `TT`, `YT`) because `lucide-react` does not provide those brand icons.
- Real search, cart state, checkout, and social URLs remain deferred to future tasks.
- Actual browser viewport screenshots/manual visual checks were not performed.
