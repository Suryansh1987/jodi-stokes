# 4. Programs and Book Sections

Status: Completed
Mandatory: Yes  
Depends On: Tasks 0-2

## Goal

Implement the training programs grid and water exercise book feature from the mock.

## Reference Mock Findings

- Programs section has three cards:
  - Strong Foundations, featured, 16 weeks, all levels, `$249 / full program`
  - Lean & Lifted, 12 weeks, intermediate, `$189`
  - The Reset, 6 weeks, beginner, `$99`
- Featured program uses `jodi-plank.png`.
- Book section has a 3D-styled cover using `book-water-exercise.png`, pricing, feature list, and three CTAs.

## Implementation Scope

- Create:
  - `components/sections/home/programs-section.tsx`
  - `components/sections/home/book-section.tsx`
- Use shadcn `Card` where it fits the program cards without fighting the visual design.
- Use `next/image` for `jodi-plank.png` and `book-water-exercise.png`.
- Use data from `lib/content/home.ts`.
- Use `lucide-react` icons for add buttons, arrows, and feature checks.
- Mark add-to-cart and buy/eBook/bulk CTAs with `[PLACEHOLDER]`.
- Mark checkout/product-detail integration with `[TODO]`.

## Files To Inspect

- `reference-html/index.html` Programs and Book sections.
- `components/ui/card.tsx`
- `lib/content/home.ts`
- `public/images/jodi/`

## Acceptance Criteria

- Program card proportions match the mock, with the first card visually featured.
- Cards have stable heights and do not jump on hover.
- Book cover keeps the 3D/perspective feel without sacrificing responsive layout.
- All CTAs are accessible and clearly marked if placeholder-only.

## Tests

- Run `npm run lint`.
- Run `npm run build`.
- Manual responsive check for 3-column, tablet, and mobile stacking behavior.

## Completion Evidence

Status: Completed

Implemented files:
- `components/sections/home/programs-section.tsx`
- `components/sections/home/book-section.tsx`
- `app/globals.css`
- `test/home.spec.ts`
- `initializeTodo/4. Programs and Book Sections.md`
- `initializeTodo/taskTracker.md`

Frontend command:
- `npm run lint`
- `npm run build`
- `PORT=3001 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 npm run test:e2e`

Frontend result: Passed

Manual verification:
- Confirmed the Programs section renders a reference-style three-card grid with the first card featured.
- Confirmed `Strong Foundations` uses `jodi-plank.png` through `next/image`.
- Confirmed `Lean & Lifted` and `The Reset` use stable CSS art panels from existing `artLabel` content.
- Confirmed program tags, prices, price note, and add buttons render from `lib/content/home.ts`.
- Confirmed the Book section renders a 3D/perspective cover using `book-water-exercise.png` through `next/image`.
- Confirmed book pricing, compare-at price, offer badge, feature checklist, and three CTAs render from `bookContent`.
- Confirmed placeholder ecommerce actions are marked with `[PLACEHOLDER]` and future checkout/download/inquiry work is marked with `[TODO]`.

Evidence:
- Replaced Task 2 placeholder program cards with shadcn `Card` shells, semantic `h3` titles, featured media, card tags, descriptions, price footer, and visual-only add-to-cart buttons.
- Replaced the placeholder book feature with the two-column 3D cover layout, price row, checked feature list, and CTA row.
- Added responsive CSS for `.programs`, `.prog-grid`, `.prog-card`, `.book`, `.book-3d`, `.book-features`, and `.cta-row`.
- Extended Playwright coverage to verify program cards, featured image loading, book cover image loading, prices, feature list, placeholder CTAs, and no horizontal overflow.
- `npm run lint` passed.
- `npm run build` passed and reported the `/` route as statically prerendered.
- `PORT=3001 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 npm run test:e2e` passed with 5 tests passed and 3 intentionally skipped by desktop/mobile project split.

Caveats:
- Program add-to-cart buttons and book CTAs remain visual-only until ecommerce/product-detail/eBook/bulk inquiry integrations are provided.
- Playwright verification used port `3001` so its production server lifecycle stayed isolated from any manually started server.
