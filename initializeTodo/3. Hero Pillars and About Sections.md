# 3. Hero, Pillars, and About Sections

Status: Completed
Mandatory: Yes
Depends On: Tasks 0-2

## Goal

Implement the first-viewport brand experience and introductory content from the mock.

## Reference Mock Findings

- Hero headline: `That's it, be fitt. Train with Jodi.`
- Hero has a badge, two CTAs, three stats, portrait image, caption, play button, and two floating result/method chips.
- Pillars section contains three equal columns: Trainer, Lifestyle, Nutrition.
- About section uses `jodi-leadership.png`, signature badge, two paragraphs, and four credentials.

## Implementation Scope

- Create:
  - `components/sections/home/hero-section.tsx`
  - `components/sections/home/pillars-section.tsx`
  - `components/sections/home/about-section.tsx`
- Use `next/image` for real images.
- Use CSS/Tailwind to preserve the mock's dark editorial layout and responsive behavior.
- Use `lucide-react` icons for CTA arrows, play, checkmarks, and pillar arrows where appropriate.
- Add accessible alt text for all images.
- Keep the hero image visually inspectable, not overly dark or blurred.
- Mark intro video action with `[PLACEHOLDER]` and future video modal with `[TODO]`.

## Files To Inspect

- `reference-html/index.html` Hero, Pillars, and About sections.
- `public/images/jodi/`
- `lib/content/home.ts`
- `app/globals.css`

## Acceptance Criteria

- First viewport clearly communicates Jodi Stokes Fitness.
- Hero CTA anchors work.
- Hero stats and floating chips remain readable on desktop and do not overlap on mobile.
- Pillars stack cleanly on small screens.
- About credentials render in a stable two-column desktop and one-column mobile layout.

## Tests

- Run `npm run lint`.
- Run `npm run build`.
- Manual visual check at desktop and mobile widths.

## Completion Evidence

Status: Completed

Implemented files:
- `components/sections/home/hero-section.tsx`
- `components/sections/home/pillars-section.tsx`
- `components/sections/home/about-section.tsx`
- `app/globals.css`
- `initializeTodo/3. Hero Pillars and About Sections.md`
- `initializeTodo/taskTracker.md`

Frontend command:
- `npm run lint`
- `npm run build`
- `npm run dev -- --port 3000`
- `curl -I http://127.0.0.1:3000`

Frontend result: Passed

Manual verification:
- Confirmed the hero renders centralized content with badge, headline, CTAs, stats, portrait image, caption, play button, and two floating chips.
- Confirmed hero CTA links still target `#programs` and `#book`.
- Confirmed the pillars band keeps `id="about"` to match the reference mock and existing nav behavior.
- Confirmed responsive CSS stacks hero/about at `max-width: 1100px`, stacks pillars, hides floating hero chips at `max-width: 720px`, and changes about credentials from two columns to one column on mobile.
- Confirmed the intro video action is explicitly marked with `[PLACEHOLDER]` and `[TODO]`.

Evidence:
- Replaced Task 2 placeholder hero markup with a reference-style first viewport using `next/image`, lucide `ArrowRight`, `Play`, `Dumbbell`, and `Sparkles` icons, stats, CTAs, caption, and result/method chips.
- Replaced placeholder pillar cards with a full-width bordered three-pillar band and hover arrow treatment.
- Replaced placeholder about markup with a two-column image/content layout, `next/image`, signature badge, long-form copy, and four credential rows with lucide checkmarks.
- Added global component CSS for hero, hero image card, floating chips, pillars, about image, signature badge, credentials, and responsive breakpoints.
- `npm run lint` passed.
- `npm run build` passed and reported the `/` route as statically prerendered.
- Dev server started at `http://localhost:3000`; escalated local HTTP smoke check returned `HTTP/1.1 200 OK`.

Caveats:
- Browser screenshot testing was not run because no browser automation binary is installed in the project. Verification used lint, production build, source inspection, static responsive CSS checks, and a local HTTP smoke check.
- The hero play button remains visual-only until a real intro video modal/player is provided.
