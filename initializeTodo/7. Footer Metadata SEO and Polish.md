# 7. Footer, Metadata, SEO, and Polish

Status: Completed
Mandatory: Yes
Depends On: Tasks 1-6

## Goal

Finish the public shell with the footer, metadata, accessibility details, and visual polish needed before final verification.

## Reference Mock Findings

- Footer repeats the brand, short description, social links, three link groups, legal links, and copyright.
- Footer columns are Shop, Train, and Studio.
- The mock includes social icons for Instagram, TikTok, YouTube, and Spotify.
- Metadata title in the mock is `Jodi Stokes Fitness - Train. Lifestyle. Nutrition.`

## Implementation Scope

- Create `components/section/shared/site-footer.tsx`.
- Add footer to the public layout or home composition.
- Update `app/layout.tsx` or route metadata for:
  - title
  - description
  - Open Graph basics if appropriate
  - viewport/theme color if useful
- Ensure all hash links resolve to existing IDs.
- Add focus-visible states for interactive elements.
- Review spacing, contrast, hover states, and mobile stacking.
- Mark social and legal links with `[PLACEHOLDER]` if URLs are fake.
- Mark future real policy/social URLs with `[TODO]`.

## Files To Inspect

- `reference-html/index.html` Footer section.
- `app/layout.tsx`
- `app/(public)/page.tsx`
- `components/section/shared/`

## Acceptance Criteria

- Footer visually matches the mock and is responsive.
- Metadata no longer uses Create Next App defaults.
- Placeholder links/actions are explicitly marked in code.
- Keyboard focus is visible across nav, cards, forms, and footer.
- Text does not overlap or overflow on common mobile widths.

## Tests

- Run `npm run lint`.
- Run `npm run build`.
- Manual keyboard tab pass through header, forms, cards, and footer.
- Manual mobile check at 390px width.

## Completion Evidence

Status: Completed

Implemented files:

- `components/section/shared/site-footer.tsx`
- `app/(public)/layout.tsx`
- `app/layout.tsx`
- `app/globals.css`
- `test/home.spec.ts`
- `initializeTodo/7. Footer Metadata SEO and Polish.md`
- `initializeTodo/taskTracker.md`

Frontend command:

- `npm run lint`
- `npm run build`
- `PORT=3001 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 npm run test:e2e`
- `git diff --check`

Frontend result:

- Passed.

Manual verification:

- Footer renders the reference-style brand block, social links, Shop/Train/Studio columns, legal links, and copyright.
- Public layout now includes a skip link, `main#main-content`, and shared footer after route content.
- Root metadata includes expanded title/description, Open Graph basics, keywords, robots, creator/publisher fields, and static dark viewport/theme-color.
- Footer grid stacks from desktop four-column layout to tablet two-column and mobile one-column layouts.
- Focus-visible styles cover footer links, skip link, buttons, and form controls.

Evidence:

- Fake social/legal/studio links are marked with `[PLACEHOLDER]` and `[TODO]` comments.
- Playwright coverage was added for footer content, metadata tags, skip link/main landmark, social/legal placeholders, representative footer hash links, hash target integrity, and horizontal overflow.

Caveats:

- Social, studio utility, and legal policy URLs remain temporary `#` placeholders.
- No canonical URL or absolute Open Graph image was added because no production domain was provided.
